import { Analytics } from "@vercel/analytics/react"
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'Chinese Zodiac Calculator - Find Your Chinese Zodiac Sign',
  description: 'Discover your Chinese zodiac sign based on your birth year. Our easy-to-use calculator helps you find your zodiac animal and learn about Chinese astrology.',
  keywords: 'Chinese zodiac, zodiac calculator, birth year calculator, Chinese astrology, zodiac animal, Chinese horoscope',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Analytics />
      </body>
    </html>
  );
}
